<Window x:Class="MindMapApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MindMap App" Height="800" Width="1200" WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>   
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Background="#F0F0F0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Мої карти" FontSize="18" FontWeight="Bold" Margin="10"/>

            <ListBox x:Name="MapsListBox" Grid.Row="1" Margin="5" SelectionChanged="MapsListBox_SelectionChanged">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,5">
                            <TextBlock Text="{Binding Title}" FontWeight="Bold"/>
                            <TextBlock Text="{Binding CreatedAt, StringFormat=d}" FontSize="10" Foreground="Gray"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <StackPanel Grid.Row="2" Margin="10">
                <TextBlock Text="Нова карта:" FontSize="12" Foreground="Gray"/>
                <TextBox x:Name="NewMapTitleBox" Height="25" Margin="0,0,0,5"/>
                <Button Content="Створити карту" Click="CreateMap_Click" Height="30" Background="#DDDDDD"/>
            </StackPanel>
        </Grid>

        <Border Grid.Column="1" BorderBrush="Gray" BorderThickness="1,0,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Background="#EFEFEF" Padding="5">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="CurrentMapLabel" Text="Оберіть карту" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,20,0"/>

                        <StackPanel Orientation="Horizontal" x:Name="ToolsPanel" IsEnabled="False">
                            <Button x:Name="BtnSelect"  Content="🖱️ Виділення" Click="SelectTool_Click" Margin="0,0,5,0" Padding="5,2"/>
                            <Button x:Name="BtnConnect" Content="🔗 З'єднати"   Click="ConnectTool_Click" Margin="0,0,5,0" Padding="5,2"/>
                            <Button x:Name="BtnDelete"  Content="❌ Видалити"  Click="DeleteTool_Click" Margin="0,0,5,0" Padding="5,2"/>

                            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="5,0"/>

                            <Button Content="➕ Новий вузол" Click="AddNode_Click" Padding="5,2" Name="BtnAddNode"/>
                            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="5,0"/>
                            <Button Content="➕ Групувати елементи" Click="BtnCreateRegion_Click" Padding="5,2" Name="BtnAddRegion"/>

                        </StackPanel>
                        <TextBlock Text="Стиль ліній:" VerticalAlignment="Center" Margin="10,0,5,0"/>

                        <ComboBox x:Name="LineStyleComboBox" Width="120" SelectedIndex="0" SelectionChanged="LineStyleComboBox_SelectionChanged">
                            <ComboBoxItem Content="Пряма"/>
                            <ComboBoxItem Content="Крива"/>
                            <ComboBoxItem Content="Ламана "/>
                        </ComboBox>
                        <Button x:Name="BtnCloudLogin" 
        Content=" Login" 
        Width="100" Height="30" 
        Margin="10,0,0,0"
        Click="BtnCloudLogin_Click"/>
                    </StackPanel>
                </Border>

                <Canvas x:Name="DrawingCanvas" Grid.Row="1" Background="White" ClipToBounds="True" MouseLeftButtonDown="Canvas_MouseLeftButtonDown" MouseDown="DrawingCanvas_MouseDown"
        MouseUp="DrawingCanvas_MouseUp"
        MouseMove="DrawingCanvas_MouseMove"/>

            </Grid>
        </Border>
    </Grid>
</Window>